Modules (plugins) for 117raster project
---------------------------------------

Every directory should contain one module
(class implementing the 'interface IRasterModule')
together with optional additional support
classes/forms.

Usage: include a directory in 117raster project
or its descendants. Module will be recognized and
automatically included in the 'Module' list-box.
